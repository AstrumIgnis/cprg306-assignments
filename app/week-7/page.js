import PageClient from './page-client';

export default function Page() {
    return (
        <main>
            <PageClient />
        </main>
    );
}

export const metadata = {
    title: "Shopping List",
};